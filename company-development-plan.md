# Technoledge Co. 开发计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档状态 | 执行中 |
| 基准日期 | 2026-08-13 |
| 适用范围 | `packages/company` 及其后续运行基础设施 |
| 推进方式 | 按阶段依赖和验收门禁推进，不以功能数量判断完成 |
| 最终决策与验收 | 总经理 |
| 实施责任 | 每个任务指定唯一责任人；M3 前由启动期开发者执行，M3 后可按运行拓扑委派 |
| 当前进度 | 组织模型和核心数据契约已完成，下一步进入治理与权限模型设计 |

本文用于指导 Technoledge Co. 从静态组织定义发展为可受控运行的 Agent 公司。本文只描述开发工作，不作为公司的日常运营制度。

## 2. 建设目标

### 2.1 总体目标

在 Pi 框架上建立一个最小但完整的公司运行闭环，使公司能够：

1. 接收人类总经理提出的目标或任务。
2. 按组织关系完成任务分派、执行、汇报和审批。
3. 通过受控 Tool 访问内部状态和外部系统。
4. 持久化任务、消息、产物、成本和审批状态。
5. 对每次关键操作提供可追溯的审计记录。
6. 在超时、失败、重启或人工介入后安全恢复。

### 2.2 首个可用版本的完成标准

首个可用版本必须至少跑通一个真实业务工作流，并满足以下条件：

- 任务能够从创建进入完成、失败或取消等明确终态。
- Agent 只能调用其权限范围内的 Tool。
- 外部写操作未经规定审批不得执行。
- Agent、部门、项目和 Provider 的 Token 成本可归集和查询。
- 进程重启后，任务和审批状态不会丢失。
- 关键状态变更、Tool 调用和拒绝事件均可审计。
- 工作流具有超时、重试、取消和人工接管路径。

### 2.3 首个可用版本暂不包含

- PostgreSQL、Redis 或独立消息队列等分布式基础设施。
- 多租户和跨公司隔离。
- 通用货币会计；首版财务范围仅覆盖 Token 和相关模型成本。
- Agent 自主修改组织结构、创建新岗位或批准招聘。
- 未经审批的邮件发送、付款、发布或其他不可逆外部操作。
- 为兼容未发布接口而保留的历史实现。

## 3. 当前基线

| 能力 | 状态 | 现有产物 |
|---|---|---|
| 组织结构 | 已完成 | [`packages/company/spec/organization.yaml`](packages/company/spec/organization.yaml) |
| 组织说明 | 已完成 | [`packages/company/docs/ORGANIZATION.md`](packages/company/docs/ORGANIZATION.md) |
| 核心数据契约 | 已完成 | [`packages/company/src/contracts/`](packages/company/src/contracts/) |
| 契约语义说明 | 已完成 | [`packages/company/docs/DATA_CONTRACTS.md`](packages/company/docs/DATA_CONTRACTS.md) |
| 契约校验测试 | 已完成 | [`packages/company/test/contracts.test.ts`](packages/company/test/contracts.test.ts) |
| 治理与权限策略 | 待实施 | 尚无机器可执行策略 |
| 控制层和持久化 | 待实施 | 尚无数据库、调度器和 Artifact 存储 |
| 公司 Tools | 待实施 | 尚无受控业务入口 |
| 员工 Agent | 待实施 | 尚无 Role 目录、员工定义和生命周期 |
| 运行拓扑 | 待实施 | 只有静态汇报和工作分配关系 |
| 工作流和自动化 | 待实施 | 尚无可恢复的业务工作流 |
| 运行治理和评估 | 待实施 | 尚无预算门禁、质量评估和运行手册 |

当前组织以 `organization.yaml` 为唯一组织真相。Department 和 Position 内嵌在 Organization 中；ReportingRelationship 从 Position 的 `reportsTo` 派生，不保存第二份可变关系。核心运行对象为：

```text
Organization  Department  Position  Agent
Project       Task        Message   Artifact
Approval      ToolInvocation         CostRecord  AuditEvent
```

Role 在当前契约中仍是不透明引用，待员工 Agent 阶段再定义其目录和版本规则。

## 4. 架构原则

1. **单一事实来源**：组织关系来自 `organization.yaml`，运行类型来自 TypeBox Schema，不复制可独立修改的组织关系或类型定义。
2. **职责分离**：Agent 负责判断，Tool 负责执行，控制层负责权限与状态，工作流负责可靠性。
3. **默认拒绝**：未显式授权的 Tool、数据、通信、委派和外部操作一律拒绝。
4. **可靠性由系统保证**：定时、重试、超时、审批、幂等和取消不得依赖 Agent 自己记忆。
5. **审计默认开启**：关键读取、状态变更、Tool 调用、审批决定和权限拒绝均写入审计记录。
6. **凭证与 Agent 隔离**：Agent 只使用受控 Tool，不直接读取密钥、连接数据库或调用外部 Provider。
7. **先跑通垂直闭环**：优先完成一个端到端工作流，再扩充部门、Tool 和基础设施。
8. **基础设施按需升级**：首版使用单进程和 SQLite；只有并发、可靠性或部署需求被实际证明后才引入分布式组件。

## 5. 总体路线

| 阶段 | 名称 | 状态 | 主要结果 | 前置阶段 |
|---|---|---|---|---|
| 1 | 组织运行模型 | 已完成 | 机器可读的部门、岗位、汇报和工作分配关系 | 无 |
| 2 | 核心数据契约 | 已完成 | 统一的静态组织与运行记录类型 | 阶段 1 |
| 3 | 治理与权限模型 | 待实施 | 可执行的授权、审批、预算和通信规则 | 阶段 1、2 |
| 4 | 最小控制层 | 待实施 | 持久化、状态机、审计、调度和 Artifact 管理 | 阶段 3 |
| 5 | 受控 Tools | 待实施 | Agent 访问公司能力的唯一入口 | 阶段 4 |
| 6 | 员工 Agent | 待实施 | Role、员工定义、实例绑定和生命周期 | 阶段 3、5 |
| 7 | 运行拓扑 | 待实施 | 静态组织关系到运行时通信和委派关系的绑定 | 阶段 3、6 |
| 8 | 工作流与自动化 | 待实施 | 可恢复的端到端业务闭环 | 阶段 4—7 |
| 9 | 运行治理与评估 | 待实施 | 上线门禁、质量评估、恢复和人工接管机制 | 阶段 8 |

基础治理规则在阶段 3 前置定义，以约束基础设施和 Tool 设计；运行治理在阶段 9 对已跑通的系统进行加固和验收。两者目的不同，不应合并到最后一次性补充。

## 6. 分阶段实施计划

### 6.1 阶段 1：组织运行模型

**状态**：已完成。

**目标**：把公司架构转换为机器可验证的组织定义。

**已完成工作**：

- [x] 定义部门、岗位、人员类型和编制范围。
- [x] 定义岗位汇报关系和工作分配来源。
- [x] 定义 CEO 作为人类岗位端点。
- [x] 定义研发岗位池及开发、测试、安全职责覆盖要求。
- [x] 提供机器可读 YAML 和人类可读说明。
- [x] 校验引用、唯一性、编制范围和汇报环路。

**验收结论**：`organization.yaml` 可被严格解析，组织说明与机器定义一致。

### 6.2 阶段 2：核心数据契约

**状态**：已完成。

**目标**：让数据库、Tool、Agent 和工作流使用同一种业务语言。

**已完成工作**：

- [x] 使用 TypeBox Schema 作为 TypeScript 类型和运行时校验的唯一来源。
- [x] 定义组织、协作、治理、成本和审计对象。
- [x] 统一 ID、时间戳、引用和 JSON 序列化规则。
- [x] 定义 Task、Approval 和 ToolInvocation 的终态约束。
- [x] 提供 YAML 解析、组织语义校验和汇报关系派生能力。
- [x] 提供契约单元测试和语义说明。

**验收结论**：所有核心对象均可严格校验；未声明字段和非法状态组合会被拒绝。

### 6.3 阶段 3：治理与权限模型

**状态**：待实施。

**目标**：在实现数据库和 Tool 前，明确谁能对什么资源执行什么操作，以及何时需要审批。

**工作项**：

- [ ] 建立操作分类：读取、内部写入、外部可逆写入、外部不可逆写入、管理操作。
- [ ] 建立资源分类：组织、Agent、项目、任务、消息、产物、成本、凭证和外部服务。
- [ ] 为每个 Position 定义 Tool、数据和作用域权限。
- [ ] 定义通信、委派、协助请求和跨部门升级规则。
- [ ] 定义审批类型、审批人、过期、撤销和重复请求规则。
- [ ] 定义任务、Agent、部门和项目的预算归属及超限行为。
- [ ] 定义授权判定结果，至少包含允许或拒绝、原因和策略版本。
- [ ] 定义凭证引用、加载、轮换、日志脱敏和泄漏处置边界。
- [ ] 将规则保存为版本化、机器可校验的规范，并提供人类可读说明。

**交付物**：

- 版本化治理规范。
- Position × Tool × Resource 的权限矩阵。
- 审批矩阵和外部副作用分级表。
- 授权判定接口及允许、拒绝测试用例。

**验收门禁**：

- 每个计划内 Tool 都有明确调用者、资源范围和审批规则。
- 没有通过岗位名称、ID 前缀或 Prompt 文本隐式推断权限的实现。
- 未匹配任何规则时返回拒绝，并产生不包含敏感输入的审计事件。
- 组织关系与权限关系分离；汇报关系本身不自动授予操作权限。

### 6.4 阶段 4：最小控制层

**状态**：待实施。

**目标**：建立公司运行状态、权限执行和可靠性机制的最小基础设施。

**首版技术边界**：单进程、SQLite、本地或可替换的 Artifact 存储、外部密钥管理引用。

**工作项**：

- [ ] 定义存储接口、SQLite Schema 和版本化迁移机制。
- [ ] 持久化 Agent、Project、Task、Message、Artifact 元数据、Approval、ToolInvocation、CostRecord 和 AuditEvent。
- [ ] 保存组织配置版本或摘要用于审计，但不建立可独立修改的汇报关系副本。
- [ ] 为任务、审批和 Tool 调用实现合法状态转换与并发保护。
- [ ] 为创建任务、提交审批和外部写操作提供幂等键。
- [ ] 实现只追加审计日志和成本账本。
- [ ] 实现进程内调度、超时、取消和有限重试能力。
- [ ] 实现消息收件箱和 Artifact URI 管理。
- [ ] 将凭证保存在控制层之外，数据库只保存不敏感的引用。

**交付物**：

- 控制层接口及 SQLite 实现。
- 数据库迁移和初始化流程。
- 状态转换、事务、重启恢复和并发测试。
- 本地开发配置和数据清理说明。

**验收门禁**：

- 进程重启后，未完成任务、待审批记录和调度信息可以恢复。
- 同一幂等请求不会重复创建任务或重复执行外部副作用。
- 非法状态转换被拒绝并审计。
- AuditEvent 只能追加，CostRecord 能按 Agent、部门、项目和 Provider 汇总。
- 数据库、日志和 Artifact 元数据中不存在明文密钥。

### 6.5 阶段 5：受控 Tools

**状态**：待实施。

**目标**：将所有公司能力封装为可授权、可校验、可审计的 Tool。

**实现顺序**：

1. 组织读取：`get_org_chart`、`get_position`、`list_direct_reports`。
2. 任务管理：`create_task`、`delegate_task`、`get_task_status`、`report_task_result`。
3. 内部通信：`send_message`、`read_inbox`、`request_assistance`。
4. 财务查询：`query_cost_ledger`、`generate_token_cost_report`。
5. 审批管理：`request_approval`、`get_approval_status`、`record_approval_decision`。
6. 外部操作：先实现 `draft_*`，再实现需要审批的 `send_*` 或 `publish_*`。

**每个 Tool 必须声明**：

- 输入、输出和错误契约。
- 允许调用的 Position 或 Agent 范围。
- 可访问的资源及作用域。
- 只读、内部写入或外部副作用级别。
- 审批、预算、频率和幂等要求。
- 审计内容及敏感字段脱敏规则。

**验收门禁**：

- Tool 输入和输出均通过运行时契约校验。
- 授权在执行入口统一完成，Tool 实现不得自行绕过。
- 允许、拒绝、超预算、待审批和执行失败路径均有测试。
- `draft_*` 与产生真实外部副作用的 Tool 分离。
- Agent 无法通过 Tool 之外的路径访问数据库或凭证。

### 6.6 阶段 6：员工 Agent

**状态**：待实施。

**目标**：将组织中的 Position 落地为具备明确职责和受限能力的员工 Agent。

**工作项**：

- [ ] 定义 Role 目录、版本规则和 Position 到 Role 的绑定方式。
- [ ] 定义 Agent 实例注册、启用、停用、替换和岗位绑定流程。
- [ ] 为每个首批岗位编写员工定义。
- [ ] 在员工定义中明确身份、职责、决策原则、工作流程、汇报格式、升级条件和 Tool 白名单。
- [ ] 将模型选择、凭证、组织拓扑和 Tool 实现保留在运行配置或控制层中。
- [ ] 首先启用完成首个业务工作流所需的最小员工集合，再扩展其余岗位。

**员工定义不得包含**：

- 数据库地址、API Key 或 Provider Token。
- 复杂组织关系的唯一副本。
- 权限判定逻辑或 Tool 具体实现。
- 依赖 Agent 自行记忆的定时、重试或审批状态。

**验收门禁**：

- 每个运行中 Agent 都能追溯到唯一 Position 和已发布 Role 版本。
- Agent 看到的 Tool 集合与权限策略一致。
- 更换 Agent 实例不会改变岗位的组织身份、历史任务或审计链。
- Prompt 注入不能扩大 Tool 权限或暴露凭证。

### 6.7 阶段 7：运行拓扑

**状态**：待实施。

**目标**：把前期定义的组织和权限关系绑定为可运行的通信、委派和汇报拓扑。

**工作项**：

- [ ] 从 Organization 解析 Position、汇报对象和工作分配来源。
- [ ] 将治理规范中的通信、委派、审批和升级规则绑定到 Agent 实例。
- [ ] 为人类总经理提供稳定的岗位端点和人工决策入口。
- [ ] 实现最大委派深度、循环委派检测和目标存在性检查。
- [ ] 定义 Agent 不可用、岗位空缺和跨部门协助时的降级路径。
- [ ] 在组织配置或策略版本变化时重新校验运行拓扑。

**验收门禁**：

- 所有运行时关系均可追溯到组织定义和治理策略。
- 未授权的直接通信、委派和跨部门访问被拒绝并审计。
- 不存在循环委派，任务不能被委派给不存在或未启用的岗位。
- 组织或策略变化导致拓扑无效时，系统拒绝启用新配置而不破坏现有运行状态。

### 6.8 阶段 8：工作流与自动化

**状态**：待实施。

**目标**：通过一个真实垂直场景验证组织、控制层、Tool、Agent 和拓扑能够协同运行。

**首个业务工作流**：月度 Token 成本报告。

```text
调度器创建月度报告任务
  → Token 财务专员读取组织与成本账本
  → 按部门、项目、Agent 和 Provider 汇总成本
  → 生成报告 Artifact
  → 向大秘书提交结果和异常说明
  → 大秘书汇总并提交总经理
  → 如需外部发送，创建审批请求
  → 总经理批准后执行发送
  → 归档任务、报告、成本依据和审计记录
```

**工作项**：

- [ ] 定义工作流状态机、输入、输出、责任岗位和验收条件。
- [ ] 定义调度周期、时区、补跑和重复触发规则。
- [ ] 为每一步定义超时、重试、取消、幂等和人工接管行为。
- [ ] 将审批作为可持久化等待状态，不占用 Agent 执行上下文。
- [ ] 使用模拟成本和外部 Provider 完成端到端测试。
- [ ] 在测试稳定后，以受限真实数据执行一次人工监督的试运行。

**验收门禁**：

- 正常路径能生成可核对的报告并完成归档。
- 重启、重复触发、Tool 失败、审批拒绝和 Agent 不可用均有确定结果。
- 未获批准时不会发生外部发送。
- 报告中的汇总值可追溯到 CostRecord，所有关键步骤可追溯到 AuditEvent。
- 试运行不依赖真实付费模型完成自动化测试。

### 6.9 阶段 9：运行治理与评估

**状态**：待实施。

**目标**：在正式运行前补齐资源、安全、质量和故障恢复门禁。

**工作项**：

- [ ] 配置最大并发 Agent 数、最大委派深度和任务超时。
- [ ] 配置任务、Agent、部门和项目的 Token 或金额预算。
- [ ] 建立异常成本、连续失败、任务积压和审批超时告警。
- [ ] 定义输出质量评估集、通过阈值和回归测试流程。
- [ ] 验证数据访问控制、日志脱敏、凭证轮换和外部副作用审批。
- [ ] 建立备份、恢复、取消、人工接管和紧急停机流程。
- [ ] 编写运行手册，记录责任人、诊断步骤和升级路径。

**验收门禁**：

- 预算、并发、权限和审批限制无法通过 Agent 指令绕过。
- 关键工作流达到已定义的质量阈值。
- 至少完成一次重启恢复、备份恢复和人工接管演练。
- 运行手册覆盖常见失败场景，并能从告警定位到相关任务和审计记录。
- 总经理完成上线验收并明确仍被接受的剩余风险。

## 7. 里程碑

| 里程碑 | 包含阶段 | 完成标志 | 状态 |
|---|---|---|---|
| M1：模型基线 | 1—2 | 组织和核心对象具有唯一、可校验定义 | 已完成 |
| M2：受控状态层 | 3—4 | 权限规则和持久化控制层通过验收 | 待实施 |
| M3：可运行员工层 | 5—7 | Tool、员工 Agent 和运行拓扑可协同工作 | 待实施 |
| M4：首个业务闭环 | 8 | 月度 Token 成本报告完成端到端试运行 | 待实施 |
| M5：可正式运行 | 9 | 治理、评估、恢复和人工接管门禁通过 | 待实施 |

当前未给出日历排期，因为 Agent 数量、可用开发资源和外部集成尚未确定。每个里程碑应在前一个里程碑通过验收后再确定目标日期，避免以日期替代依赖和质量门禁。

## 8. 执行与质量要求

每个阶段按以下流程执行：

1. 将工作项拆分为可独立验收的任务，并记录目标、范围、依赖和验收条件。
2. 先更新契约或规范，再实现依赖它们的存储、Tool、Agent 或工作流。
3. 为正常、拒绝、失败、重试和恢复路径编写测试。
4. 使用模拟 Provider 完成自动化测试，不使用真实密钥或付费调用。
5. 运行相关的定向测试；代码变更完成后运行 `npm run check`。
6. 同步更新对应架构、契约和运行文档。
7. 保存验收证据，并在阶段门禁通过后更新本文状态。

单项工作只有同时满足以下条件才算完成：

- 实现符合已发布契约和治理策略。
- 关键行为及失败路径有自动化测试。
- 日志和错误信息不包含密钥或不必要的业务数据。
- 相关文档已更新，迁移或配置变更可重复执行。
- 不引入与当前阶段无关的基础设施或兼容层。

## 9. 主要风险

| 风险 | 具体表现 | 应对措施 |
|---|---|---|
| 权限定义过晚 | Tool 已实现后才发现调用者或审批边界不明确 | 在阶段 3 完成权限和审批矩阵，未定义权限的 Tool 不进入开发 |
| 多份组织真相 | YAML、数据库和 Agent Prompt 分别保存可修改的汇报关系 | 只以 Organization 为真相，运行时关系全部派生并记录版本 |
| Agent 承担系统可靠性 | Agent 忘记重试、审批或定时任务 | 将状态机、调度、重试和审批等待放入控制层和工作流 |
| 外部副作用重复 | 重试导致重复发送、发布或其他写操作 | 使用审批、幂等键和执行结果核验，分离 `draft_*` 与真实写 Tool |
| 委派失控 | 循环委派、无限并发或跨部门越权 | 限制委派深度和并发，执行环路检测及默认拒绝策略 |
| 成本不可归因 | 调用缺少任务、项目、部门或 Agent 上下文 | ToolInvocation 和 CostRecord 强制关联可用的工作上下文 |
| 基础设施过度设计 | 首个闭环前引入分布式数据库和队列 | 首版保持 SQLite 和单进程，以测量到的瓶颈作为升级依据 |
| Prompt 泄漏敏感信息 | 密钥、数据库地址或权限逻辑进入员工定义 | 凭证只由控制层加载，员工定义只包含职责和受限 Tool 列表 |

## 10. 待决策事项

开始相应里程碑前，必须由总经理确认以下事项：

### M2 前

- 哪些外部写操作必须由总经理审批，哪些可以委托给大秘书。
- Token 预算按任务、项目、部门和 Agent 分配时的优先级及超限行为。
- SQLite 数据、Artifact 和审计日志的存放位置、保留周期与备份要求。
- 首版使用的凭证管理方式。

### M3 前

- Role 目录的首批角色、版本发布规则和模型选择原则。
- Agent 之间采用同步请求、异步消息或两者结合的具体场景。
- 岗位空缺、Agent 不可用和跨部门协助的降级路径。

### M4 前

- 月度 Token 报告的统计周期、时区、格式和异常阈值。
- 报告仅在内部归档，还是需要通过某个外部渠道发送。
- 外部发送的审批人、收件范围和失败补偿方式。

### M5 前

- 正式运行的可用性目标、告警渠道和响应责任。
- 质量评估通过阈值和允许上线的剩余风险。
- 人工接管、紧急停机和恢复运行的最终权限人。

## 11. 下一步

下一轮开发从阶段 3 开始：

1. 以阶段 5 的 Tool 清单为输入，列出全部操作和资源类型。
2. 为现有 Position 建立最小权限、通信、委派和审批矩阵。
3. 定义统一授权判定接口及默认拒绝行为。
4. 为允许、拒绝、需审批和超预算场景编写策略测试。
5. 权限模型通过评审后，再设计阶段 4 的 SQLite Schema 和控制层接口。

核心顺序不得倒置：先定义运行规则和数据契约，再实现控制层、Tool、员工和工作流。
