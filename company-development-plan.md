# Technoledge Co. 开发计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档状态 | 执行中 |
| 基准日期 | 2026-08-13 |
| 适用范围 | `packages/company` 及其后续运行基础设施 |
| 推进方式 | 按阶段依赖和验收门禁推进，不以功能数量判断完成 |
| 最终决策与验收 | 总经理 |
| 实施责任 | 每个任务指定唯一责任人；运行拓扑启用前由启动期开发者执行，启用后可按拓扑委派 |
| 当前进度 | M1 模型基线和 M2 能力需求分析已完成，下一步定义业务规则与权限边界 |

本文用于指导 Technoledge Co. 从静态组织定义发展为可受控运行的 Agent 公司。本文只描述开发工作，不作为公司的日常运营制度。

## 2. 建设目标

### 2.1 总体目标

在 Pi 框架上建立一个最小但完整的公司运行闭环，使公司能够：

1. 接收人类总经理提出的目标或任务。
2. 按组织关系完成任务分派、执行、汇报和审批。
3. 通过受控 Tool 访问内部状态和外部系统。
4. 持久化任务、消息、产物、成本和审批状态。
5. 对每次关键操作提供可追溯的审计记录。
6. 在超时、失败、重启或人工介入后安全恢复。

### 2.2 首个可用版本的完成标准

首个可用版本必须至少跑通一个真实业务工作流，并满足以下条件：

- 任务能够从创建进入完成、失败或取消等明确终态。
- Agent 只能调用其权限范围内的 Tool。
- 外部写操作未经规定审批不得执行。
- Agent、部门、项目和 Provider 的 Token 成本可归集和查询。
- 进程重启后，任务和审批状态不会丢失。
- 关键状态变更、Tool 调用和拒绝事件均可审计。
- 工作流具有超时、重试、取消和人工接管路径。

### 2.3 首个可用版本暂不包含

- PostgreSQL、Redis 或独立消息队列等分布式基础设施。
- 多租户和跨公司隔离。
- 通用货币会计；首版财务范围仅覆盖 Token 和相关模型成本。
- Agent 自主修改组织结构、创建新岗位或批准招聘。
- 未经审批的邮件发送、付款、发布或其他不可逆外部操作。
- 为兼容未发布接口而保留的历史实现。

## 3. 当前基线

| 能力 | 状态 | 现有产物 |
|---|---|---|
| 组织结构 | 已完成 | [`packages/company/spec/organization.yaml`](packages/company/spec/organization.yaml) |
| 组织说明 | 已完成 | [`packages/company/docs/ORGANIZATION.md`](packages/company/docs/ORGANIZATION.md) |
| 核心数据契约 | 已完成 | [`packages/company/src/contracts/`](packages/company/src/contracts/) |
| 契约语义说明 | 已完成 | [`packages/company/docs/DATA_CONTRACTS.md`](packages/company/docs/DATA_CONTRACTS.md) |
| 契约校验测试 | 已完成 | [`packages/company/test/contracts.test.ts`](packages/company/test/contracts.test.ts) |
| 岗位业务参考目录 | 已完成首轮分析，持续维护 | [`packages/company/docs/BUSINESS_CATALOG.md`](packages/company/docs/BUSINESS_CATALOG.md) |
| 业务规则与权限 | 待实施 | 尚未明确各岗位能做什么、做到什么范围、何时需要审批 |
| 运行记录与任务控制 | 待实施 | 尚无数据库、任务状态管理、调度器和 Artifact 存储 |
| 公司 Tools | 待实施 | 尚无受控业务入口 |
| 员工 Skills | 待实施 | 尚未从典型业务中分析可复用的知识、方法和模板 |
| 员工 Agent | 待实施 | 尚无 Role 目录、员工定义和生命周期 |
| 运行拓扑 | 待实施 | 只有静态汇报和工作分配关系 |
| 工作流和自动化 | 待实施 | 尚无可恢复的业务工作流 |
| 运行治理和评估 | 待实施 | 尚无预算门禁、质量评估和运行手册 |

当前组织以 `organization.yaml` 为唯一组织真相。Department 和 Position 内嵌在 Organization 中；ReportingRelationship 从 Position 的 `reportsTo` 派生，不保存第二份可变关系。核心运行对象为：

```text
Organization  Department  Position  Agent
Project       Task        Message   Artifact
Approval      ToolInvocation         CostRecord  AuditEvent
```

Role 在当前契约中仍是不透明引用，待员工 Agent 阶段再定义其目录和版本规则。

## 4. 架构原则

1. **单一事实来源**：组织关系来自 `organization.yaml`，运行类型来自 TypeBox Schema，不复制可独立修改的组织关系或类型定义。
2. **职责分离**：Agent 负责判断，Tool 负责执行，运行系统负责检查权限和保存状态，工作流负责重试、超时和恢复。
3. **默认拒绝**：未显式授权的 Tool、数据、通信、委派和外部操作一律拒绝。
4. **可靠性由系统保证**：定时、重试、超时、审批、幂等和取消不得依赖 Agent 自己记忆。
5. **审计默认开启**：关键读取、状态变更、Tool 调用、审批决定和权限拒绝均写入审计记录。
6. **凭证与 Agent 隔离**：Agent 只使用受控 Tool，不直接读取密钥、连接数据库或调用外部 Provider。
7. **先跑通垂直闭环**：优先完成一个端到端工作流，再扩充部门、Tool 和基础设施。
8. **基础设施按需升级**：首版使用单进程和 SQLite；只有并发、可靠性或部署需求被实际证明后才引入分布式组件。
9. **保留员工自主性**：岗位目标、权限和安全要求规定责任与边界，不规定唯一工作方法。Agent 可根据实际情况自行分析、规划、选择 Skills 和 Tools、调整执行顺序并发起必要协作。
10. **业务目录只用于能力分析**：业务类型是非穷举、可持续更新的参考材料，只用于发现可能需要的 Tools、Skills、数据和协作能力，不作为 SOP、任务白名单、权限依据或运行时流程定义。

## 5. 总体路线

| 阶段 | 名称 | 状态 | 主要结果 | 前置阶段 |
|---|---|---|---|---|
| 1 | 组织运行模型 | 已完成 | 机器可读的部门、岗位、汇报和工作分配关系 | 无 |
| 2 | 核心数据契约 | 已完成 | 统一的静态组织与运行记录类型 | 阶段 1 |
| 3 | 业务细化 | 已完成首轮分析，持续维护 | 供人分析 Tools、Skills 等能力需求的岗位业务参考目录 | 阶段 1、2 |
| 4 | 业务规则与权限边界 | 待实施 | 明确谁能做什么、做到什么范围、何时需要审批 | 阶段 3 |
| 5 | 运行记录与任务控制 | 待实施 | 保存任务、审批、成本和审计记录，并保证任务可靠推进 | 阶段 4 |
| 6 | Tools 与 Skills | 待实施 | 为 Agent 提供受控执行能力和可选工作方法 | 阶段 5 |
| 7 | 员工 Agent | 待实施 | Role、员工定义、能力配置、实例绑定和生命周期 | 阶段 4、6 |
| 8 | 运行拓扑 | 待实施 | 静态组织关系到运行时通信和委派关系的绑定 | 阶段 4、7 |
| 9 | 工作流与自动化 | 待实施 | 可恢复的端到端业务闭环 | 阶段 5—8 |
| 10 | 运行治理与评估 | 待实施 | 上线门禁、质量评估、恢复和人工接管机制 | 阶段 9 |

阶段 3 通过典型业务场景分析“员工可能需要哪些能力”，但不规定员工只能做这些业务，也不规定固定步骤。阶段 4 只明确访问资源、外部操作、预算和协作等必要边界；阶段 5 再决定系统需要保存和控制什么。阶段 10 对已经跑通的系统进行上线前加固和验收。

## 6. 分阶段实施计划

### 6.1 阶段 1：组织运行模型

**状态**：已完成。

**目标**：把公司架构转换为机器可验证的组织定义。

**已完成工作**：

- [x] 定义部门、岗位、人员类型和编制范围。
- [x] 定义岗位汇报关系和工作分配来源。
- [x] 定义 CEO 作为人类岗位端点。
- [x] 定义研发岗位池及开发、测试、安全职责覆盖要求。
- [x] 提供机器可读 YAML 和人类可读说明。
- [x] 校验引用、唯一性、编制范围和汇报环路。

**验收结论**：`organization.yaml` 可被严格解析，组织说明与机器定义一致。

### 6.2 阶段 2：核心数据契约

**状态**：已完成。

**目标**：让数据库、Tool、Agent 和工作流使用同一种业务语言。

**已完成工作**：

- [x] 使用 TypeBox Schema 作为 TypeScript 类型和运行时校验的唯一来源。
- [x] 定义组织、协作、治理、成本和审计对象。
- [x] 统一 ID、时间戳、引用和 JSON 序列化规则。
- [x] 定义 Task、Approval 和 ToolInvocation 的终态约束。
- [x] 提供 YAML 解析、组织语义校验和汇报关系派生能力。
- [x] 提供契约单元测试和语义说明。

**验收结论**：所有核心对象均可严格校验；未声明字段和非法状态组合会被拒绝。

### 6.3 阶段 3：业务细化

**状态**：已完成首轮分析，持续维护。

**问题**：当前组织结构只说明岗位职责的大方向，还不足以分析后续可能需要哪些数据、Tools、Skills 和协作能力。例如“Token 财务专员负责成本管理”没有提供足够的典型场景，无法判断他是否需要成本查询 Tool、报告分析 Skill 或异常调查能力。

**目标**：按岗位列出可能重复出现的典型业务，并分析这些场景可能需要的输入、产出、Tools、Skills、数据和协作能力。这里的“业务类型”只是供人分析和规划的标签，不是运行时对象。

业务参考目录按 Position 整理，而不是绑定具体 Agent 实例。它反映岗位常见的工作环境和能力需求，不定义岗位业务范围的穷举集合。

**首版业务类型**：

| 岗位 | 业务类型 |
|---|---|
| 总经理 | 重大经营决策、关键审批、资源分配、异常事项和人工接管 |
| 大秘书 | 任务受理与分流、跨部门协调、公司级汇报、决策跟进、风险汇总与升级 |
| 运营保障专员 | 故障诊断与处置、灾备 |
| Token 财务专员 | Token 成本记录、周期报告、异常消耗调查 |
| 人力规划专员 | 工作负载分析、能力缺口分析、增员建议 |
| Agent 培训专员 | 岗位规则细化 |
| 项目经理 | 需求澄清、项目计划、任务拆分与分派、进度与风险管理、成果验收与项目汇报 |
| 研发岗位池 | 方案设计、开发实现、测试验证、安全评估、缺陷修复、技术文档和专业分析 |

**使用边界**：

- 目录是非穷举的参考样本。未列出的业务不因此被禁止，Agent 可以在岗位职责内识别和处理新情况。
- 目录不是 SOP，不规定固定步骤、固定 Tool 组合或唯一产出形式。
- 目录不是任务白名单、权限配置、绩效指标或 Agent Prompt 中的强制流程。
- 真正的硬边界只来自岗位责任、数据与 Tool 权限、安全要求、预算和必要审批。
- Agent 在硬边界内保留主观能动性，自行判断问题、制定方案、选择能力、寻求协作和调整执行方式。

这张表是分析起点。对每个典型业务，只需补充足以发现能力需求的信息：

- 典型目标和可能的任务来源。
- 常见输入、背景信息和预期结果。
- 可能涉及的协作岗位和外部对象。
- 需要读取或修改的数据类别。
- 可能需要的 Tool、Skill、模板、知识和外部服务。
- 常见风险、成本、审批点和需要人工判断的位置。
- Agent 需要保留自主判断的变化点和例外情况。

例如，“按需 Token 汇总报告”可以作为能力分析样本：它提示系统可能需要成本查询 Tool、组织归属查询、财务分析 Skill 和报告结果。该样本不要求 Token 财务专员始终按固定顺序执行；员工可根据 CEO 的问题、数据质量和统计范围自行调整分析方法。

**已完成工作**：

- [x] 建立覆盖现有岗位的典型业务参考目录，不将其视为岗位全部业务的穷举。
- [x] 记录典型业务的目标、输入、结果、协作、变化点和自主判断空间。
- [x] 标出跨岗位协作、信息缺口、待决策事项和员工自主判断边界。
- [x] 汇总后续可能需要的数据、Tools、Skills、审批和运行记录候选。
- [x] 区分模型基础能力、可选 Skill、受控 Tool 和后续规则需求。
- [x] 将分析结果整理到 `packages/company/docs/BUSINESS_CATALOG.md`。

**完成结论**：首轮业务目录已经足以支持后续 Tools、Skills、数据、协作和运行能力分析。该目录是持续维护的公司认知材料，不是 SOP、任务白名单或固定工作流；暂缓、合并和待决策内容均属于有效分析结论，不影响 M2 完成。

### 6.4 阶段 4：业务规则与权限边界

**状态**：待实施。

**问题**：业务目录只能帮助发现能力需求，不能用来判断员工是否有权行动。例如，目录列出 Token 成本报告，不代表 Token 财务专员可以自行将报告发送给外部收件人；反过来，目录没有列出某种新的分析任务，也不代表员工不能处理它。

**目标**：围绕数据访问、Tool 调用、外部影响、预算和组织责任定义必要的硬边界。权限控制约束的是员工可以影响的资源和外部结果，不约束员工如何思考、如何安排工作，也不把业务目录变成行动白名单。

**需要形成的四类规则**：

1. **操作规则**：谁可以读取、创建、修改或取消哪些数据和任务。
2. **协作规则**：谁可以联系谁、给谁分派任务、向谁请求协助或升级问题。
3. **审批规则**：哪些动作必须先批准、谁批准、批准多久有效、拒绝后如何处理。
4. **资源规则**：每个任务、项目、部门或 Agent 可使用多少 Token，超出后停止、告警还是申请追加。

**工作项**：

- [ ] 参考业务目录识别需要访问的数据、Tools、外部系统和高风险操作，不要求枚举员工可能采取的全部行动。
- [ ] 为受控资源和 Tool 填写“允许岗位、允许范围、前置条件、是否审批、超限处理”。
- [ ] 定义跨部门通信、委派、协助请求和问题升级规则。
- [ ] 区分内部读取、内部修改、可撤销外部操作和不可撤销外部操作。
- [ ] 定义凭证由谁配置和使用，明确 Agent 不得直接读取凭证。
- [ ] 规定没有匹配规则时一律拒绝，并记录拒绝原因。
- [ ] 先形成人类可读规则表，评审通过后再转换为版本化、机器可校验的配置。

**交付物**：

- 岗位操作与数据范围表。
- 通信和任务分派关系表。
- 审批事项与审批人表。
- Token 预算与超限处理表。
- 与以上规则一致的机器可读配置和规则测试。

**验收门禁**：

- 阶段 3 识别出的受控资源、Tool 和外部操作都有明确的允许、拒绝或需审批结论。
- 每条规则能用具体业务例子解释，不依赖岗位名称或 Prompt 文本进行隐式判断。
- 汇报关系不自动等于操作权限；业务类型也不自动等于操作权限。
- 权限只约束资源访问和产生影响的操作，不约束员工在岗位职责内的分析、规划和工作方法。
- 对受控资源或 Tool 的操作没有匹配规则时会被拒绝，并生成不包含敏感数据的记录。
- 人类可读规则与机器可读配置使用同一版本，内容一致。

### 6.5 阶段 5：运行记录与任务控制

**状态**：待实施。

**问题**：明确权限后，系统仍然需要记住公司正在做什么，并保证工作不会因进程重启、重复请求或临时失败而丢失或重复执行。例如，月度报告任务正在等待总经理审批时，如果程序重启，任务必须仍处于等待审批状态；重试发送操作时，也不能把同一封邮件发送两次。

**目标**：建立一个简单的运行系统，保存任务、消息、审批、成本、产物和操作记录，并按照明确规则推进任务。首版采用单进程和 SQLite，不引入分布式数据库或消息队列。

**系统需要完成的事情**：

1. 保存公司当前有哪些 Agent、项目、任务、消息、审批和产物。
2. 只允许合法的任务变化，例如“待执行 → 执行中 → 已完成”，拒绝从“已完成”重新回到“执行中”。
3. 保存每次 Tool 调用、Token 成本和关键操作记录，便于查询和追责。
4. 在指定时间创建任务，并处理超时、取消和有限次数的重试。
5. 识别重复请求，避免重复创建任务或重复执行外部操作。
6. 在程序重启后恢复尚未完成的任务和待审批事项。

这里的“运行记录”指需要长期保存的公司工作事实；“任务控制”指检查任务能否进入下一状态，并处理调度、超时、重试和恢复。它们不是 Agent 的业务判断。

**工作项**：

- [ ] 定义 SQLite 表、存储接口和数据库升级流程。
- [ ] 保存 Agent、Project、Task、Message、Artifact 元数据、Approval、ToolInvocation、CostRecord 和 AuditEvent。
- [ ] 保存组织配置的版本或摘要用于追溯，但不复制一份可单独修改的汇报关系。
- [ ] 明确任务、审批和 Tool 调用分别允许怎样改变状态。
- [ ] 为创建任务、提交审批和外部操作增加重复请求保护。
- [ ] 让审计和成本记录只能追加，不能覆盖历史。
- [ ] 实现进程内定时任务、超时、取消和有限重试。
- [ ] 实现消息收件箱和 Artifact URI 管理。
- [ ] 数据库只保存凭证引用，不保存明文密钥。

**交付物**：

- 运行记录和任务控制接口及其 SQLite 实现。
- 数据库初始化和升级流程。
- 任务状态规则说明。
- 状态变化、重复请求保护、重启恢复和并发测试。
- 本地开发配置和数据清理说明。

**验收门禁**：

- 进程重启后，未完成任务、待审批记录和定时信息可以恢复。
- 重复请求不会重复创建任务或重复执行外部操作。
- 不允许的状态变化被拒绝并留下记录。
- 审计记录只能追加，成本能按 Agent、部门、项目和 Provider 汇总。
- 数据库、日志和 Artifact 元数据中不存在明文密钥。

### 6.6 阶段 6：Tools 与 Skills

**状态**：待实施。

**目标**：根据典型业务分析补齐 Agent 需要的执行能力和可选工作方法。Tool 用于读取状态、修改系统或连接外部服务；Skill 用于提供可复用的知识、分析方法、检查清单和模板。Agent 根据实际任务自行决定是否使用某项 Skill 或 Tool，以及如何组合使用。

不应把所有业务都实现成 Tool 或 Skill。若某项工作只需要 Agent 的通用分析能力，就不增加额外抽象；只有涉及确定性系统操作时才实现 Tool，只有存在可复用专业方法或知识时才整理为 Skill。

**实现顺序**：

1. 组织读取：`get_org_chart`、`get_position`、`list_direct_reports`。
2. 任务管理：`create_task`、`delegate_task`、`get_task_status`、`report_task_result`。
3. 内部通信：`send_message`、`read_inbox`、`request_assistance`。
4. 财务查询：`query_cost_ledger`、`generate_token_cost_report`。
5. 审批管理：`request_approval`、`get_approval_status`、`record_approval_decision`。
6. 外部操作：先实现 `draft_*`，再实现需要审批的 `send_*` 或 `publish_*`。

**Skill 候选来源**：

- Token 成本分析和报告方法。
- 故障诊断、恢复和复盘方法。
- 需求澄清、项目拆分和风险分析方法。
- Agent 上岗培训和能力评估方法。

以上只是从典型业务中得到的候选项。是否创建 Skill，要根据方法能否复用、是否超出模型通用能力以及维护成本决定。

**每个 Tool 必须声明**：

- 输入、输出和错误契约。
- 允许调用的 Position 或 Agent 范围。
- 可访问的资源及作用域。
- 只读、内部写入或外部副作用级别。
- 审批、预算、频率和幂等要求。
- 审计内容及敏感字段脱敏规则。

**验收门禁**：

- Tool 输入和输出均通过运行时契约校验。
- 授权在执行入口统一完成，Tool 实现不得自行绕过。
- 允许、拒绝、超预算、待审批和执行失败路径均有测试。
- `draft_*` 与产生真实外部副作用的 Tool 分离。
- Agent 无法通过 Tool 之外的路径访问数据库或凭证。
- Tool 和 Skill 的存在不强制 Agent 采用固定工作顺序；除非安全或外部副作用要求，Agent 可以自行选择和组合能力。

### 6.7 阶段 7：员工 Agent

**状态**：待实施。

**目标**：将组织中的 Position 落地为具备明确职责、必要能力和自主判断空间的员工 Agent。

**工作项**：

- [ ] 定义 Role 目录、版本规则和 Position 到 Role 的绑定方式。
- [ ] 定义 Agent 实例注册、启用、停用、替换和岗位绑定流程。
- [ ] 为每个首批岗位编写员工定义。
- [ ] 在员工定义中明确身份、职责、决策原则、结果要求、汇报要求、升级条件和可用 Tools、Skills。
- [ ] 明确员工可在岗位责任和硬边界内自行规划、选择方法、调整顺序、寻求协作并处理业务目录未列出的新情况。
- [ ] 将模型选择、凭证、组织拓扑和 Tool 实现保留在运行配置或运行系统中。
- [ ] 首先启用完成首个业务工作流所需的最小员工集合，再扩展其余岗位。

**员工定义不得包含**：

- 数据库地址、API Key 或 Provider Token。
- 复杂组织关系的唯一副本。
- 权限判定逻辑或 Tool 具体实现。
- 依赖 Agent 自行记忆的定时、重试或审批状态。
- 将业务参考目录改写成强制 SOP、封闭任务清单或固定思考步骤的指令。

**验收门禁**：

- 每个运行中 Agent 都能追溯到唯一 Position 和已发布 Role 版本。
- Agent 看到的 Tool 集合与权限策略一致。
- 更换 Agent 实例不会改变岗位的组织身份、历史任务或审计链。
- Prompt 注入不能扩大 Tool 权限或暴露凭证。
- Agent 能在不突破岗位责任和硬边界的前提下，自主处理业务目录未预先列出的合理任务。

### 6.8 阶段 8：运行拓扑

**状态**：待实施。

**目标**：把前期定义的组织和权限关系绑定为可运行的通信、委派和汇报拓扑。

**工作项**：

- [ ] 从 Organization 解析 Position、汇报对象和工作分配来源。
- [ ] 将业务规则中的通信、委派、审批和升级要求绑定到 Agent 实例。
- [ ] 为人类总经理提供稳定的岗位端点和人工决策入口。
- [ ] 实现最大委派深度、循环委派检测和目标存在性检查。
- [ ] 定义 Agent 不可用、岗位空缺和跨部门协助时的降级路径。
- [ ] 在组织配置或策略版本变化时重新校验运行拓扑。

**验收门禁**：

- 所有运行时关系均可追溯到组织定义和业务规则。
- 未授权的直接通信、委派和跨部门访问被拒绝并审计。
- 不存在循环委派，任务不能被委派给不存在或未启用的岗位。
- 组织或策略变化导致拓扑无效时，系统拒绝启用新配置而不破坏现有运行状态。

### 6.9 阶段 9：工作流与自动化

**状态**：待实施。

**目标**：通过一个真实垂直场景验证组织、运行记录、任务控制、Tool、Agent 和拓扑能够协同运行。

工作流只用于明确选择的、重复性高或可靠性要求高的业务，例如按需汇总和审批等待。它负责状态、重试和恢复，不替代员工的分析判断，也不要求其他临时业务套用同一套步骤。

**候选首个业务工作流**：按需 Token 汇总报告。

```text
总经理提出 Token 消耗查询请求
  → 大秘书将请求交给 Token 财务专员
  → Token 财务专员读取组织与成本账本
  → 按请求对部门和模型等维度汇总 Token
  → 说明统计范围、结果和数据缺口
  → 将结果回复总经理
  → 归档请求、查询依据、结果和审计记录
```

**工作项**：

- [ ] 定义工作流状态机、输入、输出、责任岗位和验收条件。
- [ ] 定义请求转交、统计范围和重复请求处理规则。
- [ ] 为每一步定义超时、重试、取消、幂等和人工接管行为。
- [ ] 使用模拟 `CostRecord` 和组织数据完成端到端测试。
- [ ] 在测试稳定后，以受限真实数据执行一次人工监督的试运行。

**验收门禁**：

- 正常路径能生成可核对的报告并完成归档。
- 重启、重复请求、Tool 失败和 Agent 不可用均有确定结果。
- 报告中的汇总值可追溯到 CostRecord，所有关键步骤可追溯到 AuditEvent。
- 试运行不依赖真实付费模型完成自动化测试。

### 6.10 阶段 10：运行治理与评估

**状态**：待实施。

**目标**：在正式运行前补齐资源、安全、质量和故障恢复门禁。

**工作项**：

- [ ] 配置最大并发 Agent 数、最大委派深度和任务超时。
- [ ] 配置任务、Agent、部门和项目的 Token 或金额预算。
- [ ] 建立异常成本、连续失败、任务积压和审批超时告警。
- [ ] 定义输出质量评估集、通过阈值和回归测试流程。
- [ ] 验证数据访问控制、日志脱敏、凭证轮换和外部副作用审批。
- [ ] 建立备份、恢复、取消、人工接管和紧急停机流程。
- [ ] 编写运行手册，记录责任人、诊断步骤和升级路径。

**验收门禁**：

- 预算、并发、权限和审批限制无法通过 Agent 指令绕过。
- 关键工作流达到已定义的质量阈值。
- 至少完成一次重启恢复、备份恢复和人工接管演练。
- 运行手册覆盖常见失败场景，并能从告警定位到相关任务和审计记录。
- 总经理完成上线验收并明确仍被接受的剩余风险。

## 7. 里程碑

| 里程碑 | 包含阶段 | 完成标志 | 状态 |
|---|---|---|---|
| M1：模型基线 | 1—2 | 组织和核心对象具有唯一、可校验定义 | 已完成 |
| M2：能力需求分析 | 3 | 典型业务足以支持 Tools、Skills、数据和协作能力分析 | 已完成 |
| M3：受控运行基础 | 4—5 | 业务规则、权限和运行记录系统通过验收 | 待实施 |
| M4：可运行员工层 | 6—8 | Tool、员工 Agent 和运行拓扑可协同工作 | 待实施 |
| M5：首个业务闭环 | 9 | 按需 Token 汇总报告完成端到端试运行 | 待实施 |
| M6：可正式运行 | 10 | 治理、评估、恢复和人工接管门禁通过 | 待实施 |

当前未给出日历排期，因为 Agent 数量、可用开发资源和外部集成尚未确定。每个里程碑应在前一个里程碑通过验收后再确定目标日期，避免以日期替代依赖和质量门禁。

## 8. 执行与质量要求

每个阶段按以下流程执行：

1. 将工作项拆分为可独立验收的任务，并记录目标、范围、依赖和验收条件。
2. 先更新契约或规范，再实现依赖它们的存储、Tool、Agent 或工作流。
3. 为正常、拒绝、失败、重试和恢复路径编写测试。
4. 使用模拟 Provider 完成自动化测试，不使用真实密钥或付费调用。
5. 运行相关的定向测试；代码变更完成后运行 `npm run check`。
6. 同步更新对应架构、契约和运行文档。
7. 保存验收证据，并在阶段门禁通过后更新本文状态。

单项工作只有同时满足以下条件才算完成：

- 实现符合数据契约和权限规则；新增 Tools、Skills 等能力能追溯到业务分析中的实际需求。
- 关键行为及失败路径有自动化测试。
- 日志和错误信息不包含密钥或不必要的业务数据。
- 相关文档已更新，迁移或配置变更可重复执行。
- 不引入与当前阶段无关的基础设施或兼容层。

## 9. 主要风险

| 风险 | 具体表现 | 应对措施 |
|---|---|---|
| 业务分析不足 | 开发 Tool 后才发现岗位职责、输入或交接关系不清 | 在阶段 3 先完成岗位业务目录，并用具体业务验证后续设计 |
| 业务目录僵化 | 将典型业务误写成 SOP 或任务白名单，员工只能机械执行预设流程 | 明确目录非穷举且不进入运行时判断；员工在硬边界内保留自主规划和处置权 |
| 权限定义过晚 | Tool 已实现后才发现调用者或审批边界不明确 | 在阶段 4 完成权限和审批规则，未定义权限的 Tool 不进入开发 |
| 多份组织真相 | YAML、数据库和 Agent Prompt 分别保存可修改的汇报关系 | 只以 Organization 为真相，运行时关系全部派生并记录版本 |
| Agent 承担系统可靠性 | Agent 忘记重试、审批或定时任务 | 将任务状态检查、调度、重试和审批等待放入运行系统和工作流 |
| 外部副作用重复 | 重试导致重复发送、发布或其他写操作 | 使用审批、幂等键和执行结果核验，分离 `draft_*` 与真实写 Tool |
| 委派失控 | 循环委派、无限并发或跨部门越权 | 限制委派深度和并发，执行环路检测及默认拒绝策略 |
| 成本不可归因 | 调用缺少任务、项目、部门或 Agent 上下文 | ToolInvocation 和 CostRecord 强制关联可用的工作上下文 |
| 基础设施过度设计 | 首个闭环前引入分布式数据库和队列 | 首版保持 SQLite 和单进程，以测量到的瓶颈作为升级依据 |
| Prompt 泄漏敏感信息 | 密钥、数据库地址或权限逻辑进入员工定义 | 凭证只由运行系统加载，员工定义只包含职责和受限 Tool 列表 |

## 10. 待决策事项

开始相应里程碑前，必须由总经理确认以下事项：

### M3 前

- 哪些外部写操作必须由总经理审批，哪些可以委托给大秘书。
- Token 预算按任务、项目、部门和 Agent 分配时的优先级及超限行为。
- SQLite 数据、Artifact 和审计日志的存放位置、保留周期与备份要求。
- 首版使用的凭证管理方式。

### M4 前

- Role 目录的首批角色、版本发布规则和模型选择原则。
- Agent 之间采用同步请求、异步消息或两者结合的具体场景。
- 岗位空缺、Agent 不可用和跨部门协助的降级路径。

### M5 前

- 是否以按需 Token 汇总报告作为首个端到端工作流。
- CEO 未指定时采用的默认统计范围、时区和结果格式。
- 汇总结果是即时返回，还是同时保存为 Artifact。
- 请求转交、回复路径和数据缺口的处理方式。

### M6 前

- 正式运行的可用性目标、告警渠道和响应责任。
- 质量评估通过阈值和允许上线的剩余风险。
- 人工接管、紧急停机和恢复运行的最终权限人。

## 11. 下一步

下一轮开发从阶段 4 开始：

1. 从已确认业务条目提取需要访问的数据、Tools、外部系统和高风险操作。
2. 区分员工自主判断与必须由系统执行的权限、安全、预算和审批边界。
3. 定义岗位操作范围、通信与分派关系、审批事项和 Token 预算规则。
4. 将人类可读规则转换为版本化、机器可校验的配置。
5. 为允许、拒绝和需审批场景建立规则测试，再进入运行记录与任务控制设计。

核心顺序不得倒置：在现有组织和数据契约基线上，先细化业务，再定义权限，然后实现运行记录、Tool、员工和工作流。
