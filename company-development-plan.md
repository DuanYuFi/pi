# Technoledge Co. 开发计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 状态 | 执行中 |
| 基准日期 | 2026-08-14 |
| 适用范围 | `packages/company` 及其后续运行基础设施 |
| 当前进度 | M1—M3 已完成，下一步实现 M4 最小可运行公司 |
| 最终决策与验收 | 总经理 |

本文只描述 Technoledge Co. 的开发工作，不作为公司的日常运营制度。开发按依赖和可观察结果推进，不以文档长度、功能数量或日历日期判断完成。

## 2. 首版目标

公司的首要业务是软件项目交付。总经理通过 Pi 会话与大秘书交互，大秘书把软件任务交给项目经理；项目经理使用现有 Pi Subagent 完成规划、架构、开发、测试、安全分析和复核，并把顶层结果交回大秘书。

其他岗位只负责公司运行所需的专职工作：

- Token 财务专员记录并查询 Token 消耗。
- 运营保障专员诊断和修复公司 Agent 系统。
- Agent 培训专员设计岗位并维护 Prompt、Skill、`AGENTS.md` 和 Agent 定义。

首个可用版本应满足：

1. CEO 请求可以被大秘书正确派发。
2. 软件项目可以在后台完成，不依赖原 Pi 会话持续存在。
3. 项目经理能调用多个研发 Subagent，并以测试和独立复核作为代码交付门禁。
4. 顶层任务具有完成、失败、等待 CEO 和中断后的确定处理路径。
5. 模型 Token 能自动记账，并由财务执行受限的只读 SQL 查询。
6. 越界、不可逆和外部操作不能因 Agent 委派而绕过 CEO 授权。
7. 后台结果和 fatal 异常能按确认与频率门禁通知 CEO。
8. 公司运行时可以由独立 Supervisor 启动、检查和失败回滚。

首版不包含：

- PostgreSQL、Redis、独立消息队列或分布式部署。
- 多租户和跨公司隔离。
- 通用货币会计或首版 Token 硬预算。
- 数学研究等不属于软件工程和现有支持岗位的通用业务。
- Agent 自主扩大权限、修改组织或读取凭证。
- 为未发布接口保留的兼容层。

## 3. 设计基线

| 产物 | 状态 | 位置 |
|---|---|---|
| 组织配置 | 已完成 | `packages/company/spec/organization.yaml` |
| 组织说明 | 已完成 | `packages/company/docs/ORGANIZATION.md` |
| 核心数据契约 | 已完成 | `packages/company/src/contracts/` |
| 契约语义 | 已完成 | `packages/company/docs/DATA_CONTRACTS.md` |
| 首版业务目录 | 已完成 | `packages/company/docs/BUSINESS_CATALOG.md` |
| M3 运行设计 | 已完成 | `packages/company/docs/RUNTIME_DESIGN.md` |
| 运行实现 | 待实施 | M4 |

组织关系只来自 `organization.yaml`。运行对象继续以 TypeBox Schema 为类型和运行时校验来源：

```text
Organization  Department  Position  Agent
Project       Task        Message   Artifact
Approval      ToolInvocation         CostRecord  AuditEvent
```

M4 可以根据实现需要扩展契约，但不得在数据库、Agent Prompt 或 Tool 实现中复制可独立修改的组织和权限真相。

## 4. 架构原则

1. Agent 负责分析和判断；运行系统负责权限、状态、幂等和审计。
2. 大秘书负责路由与汇总，不代替专业岗位执行。
3. 项目经理只负责软件工程，具体技术执行复用现有 Pi Agent 和 `subagent` 扩展。
4. 汇报关系、任务文本和 Prompt 均不能扩大 Tool、文件、网络、数据库或凭证权限。
5. 项目工作区内的常规开发由 CEO 原始任务授权，越界和外部副作用默认拒绝。
6. 所有代码交付都要经过测试和独立 reviewer；专项安全检查按风险决定。
7. 模型 usage 自动进入只追加账本，财务只读查询且不修改历史。
8. 后台任务与 Pi 会话解耦；重启造成的模型调用中断由责任岗位续办，不伪造恢复。
9. 运营保障形成和验证修复，运行时之外的 Supervisor 负责应用、重启和回滚。
10. 先完成一个真实软件项目闭环，再扩充业务范围和基础设施。

## 5. 里程碑

| 里程碑 | 目标 | 完成标志 | 状态 |
|---|---|---|---|
| M1：模型基线 | 定义组织和核心对象 | 组织与运行对象具有唯一、可校验定义 | 已完成 |
| M2：业务基线 | 明确首版公司做什么 | 五类首版业务及其能力需求清楚 | 已完成 |
| M3：运行设计 | 定义职责、流程、权限和组件边界 | 六个设计场景均有唯一责任方和确定结果 | 已完成，2026-08-14 |
| M4：最小可运行公司 | 实现存储、任务控制、Tools、Agent、后台运行、邮件和 Supervisor | 公司逻辑可在模拟数据和受控环境中运行 | 待实施 |
| M5：真实软件项目 | 用公司完成一个小型软件项目 | 项目从 CEO 请求进入后台，完成开发、验证、通知和成本查询 | 待实施 |
| M6：正式运行 | 完成安全、恢复、质量和人工接管加固 | 总经理接受剩余风险并批准正式运行 | 待实施 |

## 6. M1—M3 完成情况

### M1：模型基线

- 组织、岗位、汇报关系和工作分配来源具有机器可读定义。
- Agent、Project、Task、Message、Artifact、Approval、ToolInvocation、CostRecord 和 AuditEvent 具有严格运行时契约。
- 组织引用、重复 ID、编制范围和汇报环路可校验。

### M2：业务基线

- 业务目录精简为请求派发、软件项目、Token 财务、系统运维和 Agent 配置五类。
- 软件项目是公司主业务，其他岗位为专职支持岗位。
- 非代码且无匹配岗位的任务进入能力缺口处理，不由秘书或项目经理兜底。

### M3：运行设计

M3 只完成设计，不实现 SQLite、Agent、Tool、邮件或 Supervisor。已确定：

- CEO 的原始软件项目指令授权项目工作区内的常规开发。
- 项目经理可以只读预研、自主形成内部 Plan 并调用通用研发池。
- 所有代码项目执行测试和独立复核，项目经理负责最终技术验收。
- 财务使用只读 SQLite 连接，只访问成本账本和归属视图。
- 运营保障可直接修复公司系统代码，经测试和独立 reviewer 后交由外部 Supervisor 应用。
- Agent 培训专员合并岗位规划职责，常规配置可以在检查后直接应用。
- 后台顶层任务与 Pi 会话解耦，子任务结果只返回项目经理。
- CEO 邮件同时受“上一封已确认”和“至少间隔一小时”两个门禁，fatal 也不绕过。
- 单次、任务范围和长期策略授权具有不同边界，未匹配授权默认拒绝。

M3 已通过以下桌面推演：同步 Token 查询、后台项目成功、Subagent 中断续办、高危操作审批、fatal 修复与回滚、邮件聚合与确认门禁。

## 7. M4：最小可运行公司

### 7.1 实现内容

M4 将现有设计变成单进程、SQLite 驱动的最小运行系统：

1. 实现 SQLite 初始化、迁移和运行记录接口，保存核心运行对象、组织版本或摘要及必要的后台状态。
2. 实现顶层任务、父子任务、内部消息、完成回调、审批、Artifact 引用、成本和审计控制。
3. 定义并启用大秘书、项目经理、Token 财务、运营保障、Agent 培训及研发池的最小 Agent 配置。
4. 实现统一 Tool 授权入口，使岗位、任务和审批范围不能被 Prompt 或委派扩大。
5. 适配现有 Pi `subagent` 扩展，锁定版本并把技术 Agent、调用结果、usage 和公司父子任务关联。
6. 实现与 Pi 会话解耦的后台执行；重启后恢复顶层状态，将未完成模型调用标记为中断。
7. 自动追加 `CostRecord`，为财务提供只读成本账本和归属视图。
8. 实现持久邮件队列、CEO 确认、一小时间隔、事件聚合和失败保留。
9. 实现独立 Supervisor 的启动、健康检查、候选版本切换和失败回滚。
10. 运行时加载凭证，数据库、Agent 和日志只保存不透明引用或脱敏信息。

### 7.2 M4 验收

使用模拟 Provider 和测试邮箱适配器验证：

- CEO 请求能从大秘书派发到正确岗位。
- 一个模拟软件项目能进入后台，由项目经理调用研发 Subagent，经过测试和独立复核后返回顶层结果。
- 普通子任务失败由项目经理处理，不直接通知 CEO。
- 未授权外部动作被拒绝，明确的单次或任务授权只能在原范围使用。
- 运行时重启后顶层任务、审批、成本和邮件队列仍然存在，运行中的模型调用被标记为中断。
- 财务只能执行只读查询，不能访问非成本数据或修改账本。
- 邮件聚合与双门禁符合 M3 设计。
- Supervisor 可以检测失败、启动已知版本并在候选版本健康检查失败时回滚。

具体邮件 Provider、SQLite 文件位置和 Supervisor 进程管理器在 M4 开始时根据实际部署环境选择，不在 M3 预设。

## 8. M5：真实软件项目

总经理选择一个范围明确的小型软件项目，由公司完成真实端到端交付：

```text
CEO 在 Pi 提出项目
  → 大秘书派发并结束当前会话
  → 项目经理形成内部 Plan
  → 研发 Subagent 实现、测试和独立复核
  → 项目经理验收并形成报告
  → 大秘书按邮件门禁通知 CEO
  → CEO 可在 Pi 查询项目 Token 消耗
```

验收以项目实际验收条件、测试结果、独立复核、可使用成果、顶层邮件和可追溯 Token 记录为准。自动化测试不得依赖真实付费模型；真实试运行由总经理监督并明确接受成本。

## 9. M6：正式运行

M6 只对已经跑通的系统加固：

- 凭证隔离、日志脱敏和外部副作用审批。
- 数据备份、恢复、运行时重启和 Supervisor 回滚演练。
- Agent 不可用、任务积压、连续失败和邮件长期未确认的人工处理。
- 项目质量回归、独立复核有效性和 Prompt 注入边界验证。
- 紧急停机、人工接管和恢复运行手册。

总经理完成一次恢复和人工接管演练，并明确接受剩余风险后，首版才可正式运行。

## 10. 开发与质量要求

每项实现遵循：

1. 先更新契约或设计，再实现依赖代码。
2. 只实现当前里程碑需要的最小能力，不复制现有 Pi Harness 功能。
3. 正常、拒绝、失败、中断和恢复路径均有自动化测试。
4. 自动化测试使用模拟 Provider，不调用真实密钥或付费接口。
5. 修改测试文件后运行对应定向测试；代码变更完成后运行 `npm run check`。
6. 文档、配置和实现保持一致，验收通过后更新里程碑状态。

未经总经理明确决定，不在当前阶段引入分布式基础设施、额外业务部门、通用权限语言或兼容层。

## 11. 下一步

从 M4 开始，先确定最小运行内核的包结构、SQLite 边界和 Pi `subagent` 适配方式，再实现代码。M4 的第一条垂直验证应是：大秘书接收一个模拟软件项目，项目经理在后台调用研发 Agent，完成测试和独立复核，并把顶层结果加入受限邮件队列。
